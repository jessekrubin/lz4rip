# lz4rip-core

Shared types and constants for the [lz4rip](https://crates.io/crates/lz4rip) encode/decode crates.

This crate is an internal building block. Use `lz4rip` directly unless you need only the shared definitions.
